# 开发模式 API Key：由 vite 代理在服务端注入 X-API-Key 请求头（key 不进浏览器）。
# 注意：必须用非 VITE_ 前缀（SORA_API_KEY 而非 VITE_API_KEY）——
# Vite 会把所有 VITE_ 前缀变量注入 import.meta.env，从而暴露进浏览器 JS。
# 需与后端 application-local.yml 中 app.security.api-keys 配置的一致。
# 复制为 .env 后填写（.env 已在 .gitignore 中排除）。
SORA_API_KEY=your-api-key
